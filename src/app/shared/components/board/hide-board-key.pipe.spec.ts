import { HideBoardKeyPipe } from './hide-board-key.pipe';

describe('HideBoardKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new HideBoardKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
